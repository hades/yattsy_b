[project]
name = "yattsy-b"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "python-telegram-bot>=22.8",
]

[dependency-groups]
dev = [
    "ruff>=0.15.20",
]
